{
  "mcpServers": {
    "playwright": {
      "command": "mise",
      "args": [
        "exec",
        "--",
        "npx",
        "-y",
        "@playwright/mcp@latest",
        "--headless"
      ]
    }
  }
}
