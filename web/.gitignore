# career-ops web — self-contained ignore (does not touch core root .gitignore)
/node_modules
/.next*
/out
/dist
/.turbo
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store

# local QA artifacts
.playwright-mcp/
.lifecycle-browser-artifacts/

# agent session memory — internal notes, never in the public repo
.claude/
